# https://docs.netlify.com/configure-builds/overview/
[build]
  command = "npm ci && npm run build"
  publish = "dist"

# SPA fallback if you switch from HashRouter to BrowserRouter later
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
